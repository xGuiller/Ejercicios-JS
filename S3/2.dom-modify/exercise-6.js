const apps = ['Facebook', 'Netflix', 'Instagram', 'Snapchat', 'Twitter'];

const ul$$ = document.createElement("ul");

for (let i = 0; i < apps.length; i++) {
    const li$$ = apps.length[i];

    ul$$.appendChild = li$$

    console.log(ul$$);
    
}

